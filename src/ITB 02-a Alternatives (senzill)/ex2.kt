package `ITB 02-a Alternatives (senzill)`import java.util.*
fun main() {
    val scanner = Scanner(System.`in`)
    val edat = scanner.nextInt() // demana valor
    if (edat<=32) println("SI") // compara i imprimeix per pantalla
    if (edat>32) println("NO")
}